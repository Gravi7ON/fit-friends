export enum AppRoute {
  Intro = '/',
  SignIn = '/login',
  SignUp = '/register',
  LogOut = '/logout',
}

export enum NameSpaceStore {
  Workout = 'WORKOUT',
  User = 'USER',
}

export enum AuthorizationStatus {
  Auth = 'Auth',
  NoAuth = 'No auth',
  Unknown = 'Unknown',
}

export enum APIRoute {
  SignUp = '/register',
  SignIn = '/login',
}
